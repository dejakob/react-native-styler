import { createStyle } from 'react-native-styler';

createStyle({
    main: {
        title: {
            fontSize: '16h4s',
            color: 'theme:primary',
            textAlign: 'center'
        }
    }
});
